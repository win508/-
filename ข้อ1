hour = int(input ("ระยะเวลาที่จอด ชั่วโมง"))
minute = int(input ("ระยะเวลาที่จอด นาที"))
if hour < 0 or minute < 0 :
 print ("โปรดใส่จำนวนไม่ติดลบ")
else:
    total_hours = hour
    if minute < 1:
        total_hours + 1
    if total_hours <= 1:
        kajod = 0
    else: kajod = (total_hours - 1)*30
    print (kajod)
    
